# Templates use {placeholder} syntax
# data_generator.py fills placeholders with Faker + random values
# LLM was used to generate additional template variety — generator creates rows

SECURITY_ALERT:
  - "Multiple login failures detected for user {user_id}"
  - "Unauthorized access attempt from IP {ip}"
  - "Authentication error on server {server_id}"
  - "Potential security breach detected on host {host}"
  - "Brute force attack detected for account {user_id}"
  - "Suspicious activity observed from IP {ip}"
  - "Account {user_id} locked after {attempts} failed attempts"
  - "Invalid credentials provided for user {user_id} on {host}"
  - "Invalid session token detected from IP {ip}"
  - "Multiple unauthorized access attempts on server {server_id}"
  - "Login failure threshold exceeded for user {user_id}"
  - "Intrusion detected on host {host} from IP {ip}"
  - "Security alert: invalid token for session {session_id}"
  - "Failed authentication attempt #{attempts} for user {user_id}"
  - "Credential mismatch for user {user_id} from {ip}"

RESOURCE_USAGE:
  - "phys_ram={ram}MB used_ram={used_ram}MB phys_disk={disk}GB"
  - "[instance: {instance_id}] Total memory: {ram} MB, used: {used_ram} MB"
  - "cpu_usage={cpu}% memory_usage={memory}% on host {host}"
  - "disk_usage={disk_pct}% on volume {volume}"
  - "swap={swap}MB used on server {server_id}"
  - "Memory usage is critical at {memory}% on {host}"
  - "High memory consumption detected: {used_ram}MB of {ram}MB used"
  - "phys_ram={ram}MB used_ram={used_ram}MB phys_disk={disk}GB on node {node_id}"
  - "CPU exceeded threshold: {cpu}% on instance {instance_id}"
  - "Disk usage at {disk_pct}% on {host}, volume {volume}"
  - "[instance: {instance_id}] cpu={cpu}% mem={memory}% disk={disk_pct}%"
  - "Resource alert: memory exceeded on {host}, used={used_ram}MB"
  - "Node {node_id} reporting cpu={cpu}% utilization"
  - "Storage volume {volume} at {disk_pct}% capacity"
  - "System {host} swap usage critical: {swap}MB"

WORKFLOW_ERROR:
  - "Escalation rule execution failed for ticket {ticket_id} - undefined escalation level"
  - "Task assignment for TeamID {team_id} could not complete due to invalid priority level"
  - "Workflow validation error in process {process_id}"
  - "Job {job_id} aborted due to missing configuration"
  - "Invalid priority level assigned to ticket {ticket_id}"
  - "Undefined escalation path for ticket {ticket_id} in queue {queue_id}"
  - "Process {process_id} timed out after {timeout}s"
  - "Escalation failed for ticket {ticket_id} - no handler defined"
  - "Task {task_id} could not be assigned to team {team_id}"
  - "Workflow {workflow_id} failed at step {step}"
  - "Job {job_id} error: invalid configuration in stage {step}"
  - "Deadlock detected in process {process_id}"
  - "Pipeline {workflow_id} blocked at stage {step}"
  - "Task assignment error: team {team_id} queue full"
  - "Escalation rule undefined for priority {priority} in ticket {ticket_id}"

SYSTEM_EVENT:
  - "Service {service} started successfully"
  - "Backup completed for database {db}"
  - "Node {node_id} joined cluster {cluster_id}"
  - "Deployment {deploy_id} completed successfully"
  - "Instance {instance_id} created in region {region}"
  - "Health check passed for service {service} on {host}"
  - "Scheduled job {job_id} started at {timestamp}"
  - "Service {service} stopped on host {host}"
  - "Node {node_id} left cluster {cluster_id}"
  - "Backup started for database {db} at {timestamp}"
  - "Deployment {deploy_id} finished on {host}"
  - "Instance {instance_id} terminated in region {region}"
  - "Health check ok: {service} responding on port {port}"
  - "Scheduled task {task_id} completed successfully"
  - "Service {service} restarted on {host} after config update"
  